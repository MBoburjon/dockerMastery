#What is An Image:

#App binaries and dependencies
#Metadata about the image data and how to run the image

#Images are tagged

#hub.docker.com - apt package system for Containers

docker pull IMAGENAME

docker image ls

docker history IMAGENAME:version

docker image inspect IMAGENAME 
#gives JSON Metadata about image

#images are made up of file system changes and metadata and 
#each layer stored once in filesystem

docker image tag nginx bretfisher/nginx
#adding tag an existing image

cat .docker/config.json
#gives docker login AUTH

docker image push IMAGE_NAME
#to upload docker image into hub.docker.com AFTER LOGIN in

vim Dockerfile
# getting file info

ll
# listing Dockerfiles

docker image build -t customnginx .
# building docker image from Dockerfile

docker image build -t nginx-with-html .

docker build -t IMAGENAME DIRforBuildImage
# command to build image or convert Dockerfile to image with name 
